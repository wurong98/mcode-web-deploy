#!/usr/bin/env python3
import sys
from pathlib import Path

# Add package root to sys.path so it can run standalone without pip install
sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from mcode_web_deploy.cli import main

if __name__ == "__main__":
    main()
